{
  "name": "directus-hook-permissions-guard",
  "version": "1.0.6",
  "description": "Enforces manager/owner band access on CREATE for songs/setlists/albums/bands junctions and band-visibility song_notes. Returns 403 Forbidden for unauthorized users; admin bypass.",
  "type": "module",
  "directus:extension": {
    "type": "hook",
    "path": "dist/index.js",
    "source": "src/index.js",
    "host": "^11.0.0"
  },
  "devDependencies": {
    "@directus/extensions-sdk": "^17.1.0"
  },
  "scripts": {
    "prebuild": "node scripts/check-hooks-installed.js",
    "build": "npx directus-extension build",
    "dev": "npx directus-extension build --watch"
  }
}
