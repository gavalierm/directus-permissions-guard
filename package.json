{
  "name": "directus-hook-permissions-guard",
  "version": "1.0.0",
  "description": "Enforces manager/owner band access on CREATE for songs/setlists/albums/bands junctions. Returns 403 Forbidden for unauthorized users; admin bypass.",
  "type": "module",
  "directus:extension": {
    "type": "hook",
    "path": "dist/index.js",
    "source": "src/index.js",
    "host": "^11.0.0"
  },
  "devDependencies": {
    "@directus/extensions-sdk": "^12.0.0"
  },
  "scripts": {
    "build": "npx directus-extension build",
    "dev": "npx directus-extension build --watch"
  }
}
